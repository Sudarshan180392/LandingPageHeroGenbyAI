# Universal AI Copy Generator — Environment Variables
# All API keys are stored in localStorage per provider.
# No keys are needed at build time.

# Optional: default provider (groq | openai | anthropic | deepseek | perplexity | gemini)
VITE_DEFAULT_PROVIDER=groq
